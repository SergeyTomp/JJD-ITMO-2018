staffist - имитация работы с базой данных: после логина получаем ссылку на объект из Map<>staffList.
Варианты развития - посмотреть в сторону сериализации, или (после освоения xml) держать всю информацию о юзерах в xml (как вариант),
при старте кода загружать в оперативку в Map<>staffList.
UserFactory не используется, создаются объекты Staff с разным идентификатором доступа.
В юзере добавляем enum-идентификатор доступа.
(потом для гибкости настройки индивидуального доступа сделать в юзере ArrayList <enum> -идентификаторов дополнительно разрешённых админом команд).
При старте кода в классе Company создаётся:
⦁	Map <AccessLevel, ArrayList<CommandStrings>> доступов
(ключ: уровень доступа (enum) ->  значение: ArrayList с перечнем enum-идентификаторов доступных команд).
⦁	Map <String, Staff> staffList - база данных персонала
⦁	Первый админ admin 123 ADMIN
Запуск метода: по enum-идентификатору команды создаётся объект команды в CommandFactory, методу передаётся объект юзера (по enum ID команды),
метод проверяет наличие идентификатора доступа юзера в Мар<Enum,ArrayList > доступов:
по ключу уровня доступа (enum) из юзера выходим на соответствующий ArrayList с перечнем enum-идентификаторов доступных команд
и проверяем наличие там enum ID команды. Если прав нет - отказ выполнения.
